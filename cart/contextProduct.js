import {createContext} from "react";

export const ContextCart = createContext([])
export const contextProduct = {
    name: '',
    title: [],
    weight: '',
    price: '',
    number: 1,
    totalPrice: this.price * this.number
}